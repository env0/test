version: 1
deploy:
    steps:
        setupVariables:
          after:
            - aws --version
            - aws --version
            - gcloud --version
            - az --version
destroy:
    onCompletion:
      - echo "destroy-onCompletionn"
